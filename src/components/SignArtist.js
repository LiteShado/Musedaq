
// import Artists from '../components/Artists'

const SignArtist = (props) => {

    // return (
        // <div>
        //     <Artists key ={i}/>
        //         <h1>{artist.name}</h1>
        //             {artist.isSigned(artist.name) ?
        //                 <span onClick={() => props.signArtist(artist.name)}>Sign This Artist</span>
        //                 :
        //                 <span onClick={() => props.removeArtist(artist.name)}>Un-Sign Artist</span>
        //             }
        // </div>
    // )
}

export default SignArtist
